class AppConstants {
  static const String appName = 'BASS Training';

  /// Hard-coded for the scaffold. In production this comes from the
  /// logged-in user's `organisationId` (see AppUser) once auth completes,
  /// enabling the white-label multi-tenant architecture.
  static const String demoOrganisationId = 'demo-org';
}
